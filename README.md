# JSON-Refiner
JSON Refiner Tool is a lightweight Python-based application designed to convert unstructured key-value text input into a clean and well-formatted JSON object. The tool simplifies the process of transforming raw text data into structured JSON format, which is widely used in APIs, data exchange, and configuration files
